# GEO图文生产助手实施清单

依据：已完整读取用户下载目录中的《GEO图文技能_Codex完整开发提示词.txt》；当前工作区为空，无本地 AGENTS.md 或旧代码。用户已确认架构并要求连续实施。

架构：Python 3.11 独立环境；本地 SQLite 索引、JSON 原子状态、Hermes host-mediated Qwen 动作；独立第三方图片适配器。所有运行数据置于配置的外部工作目录，源库只读。没有 Web 服务，不分配前后端端口。

- [x] 数据基础：配置与路径验证、四库增量索引、解析与中文检索、产品证据批准。
- [x] 执行核心：正式规则导入、版本状态机、人工选择与数量、学习修改复盘、自动推进、桥接结构校验。
- [x] 图片与交付：参考图实际上传、请求账本、限额和未知收费暂停、图像验证、原子发布。
- [x] 宿主与说明：精简 SKILL、阶段提示词、表单、CLI、安装升级脚本、中文说明。
- [x] 验证：先写失败测试再实现，离线两模式闭环；实际 Hermes 技能加载/触发/桥接单独验证；真实图片 API 没有授权配置则不执行。
- [x] 审查并打包：独立代码审查、完整测试日志、验收矩阵、限制与未验证项、无秘密 ZIP。

模块契约：config.py 提供 load_settings(path), validate_settings(data, production=False)；libraries.py 提供 LibraryIndex(settings).update()/search(query, library_type=None, product_id=None, limit=10)/get(source_id)；providers 模块独立接受经校验的图片路径；workflow.py 管理所有状态与授权，Python 不执行资料或模型提供的命令。


实际验收：源码97项通过；Hermes发现/终端/有限Qwen桥接已验证，完整图文及视觉未验证；真实图片API未执行。详见技能docs/验收记录.md。


## v1.1 多Agent兼容扩展

- [x] 通用 geo.host.v1 文件式模型交接、能力检查、模型身份自报审计。
- [x] generic / Hermes / Codex / Claude Code 同包安装与隔离升级。
- [x] 模型中立审核标签，兼容原 qwen 值和旧配置。
- [x] 当前 Codex 会话有限真实预检交接，停在人工门；不宣称完整生产通过。
- [x] 完整自动化回归，单列真实宿主和真实图片API未验证范围。


## v1.2 用户修订

- [x] 新默认产品名称218轻便侠；不擅自重命名已登记事实及历史任务。
- [x] 仅开始任务、配置任务两个启动入口；任务内控制按上下文处理。
- [x] API明确纳入统一配置表，保留本地安全凭据接入。
- [x] 源码、模板、中文操作说明和回归测试同步更新。
