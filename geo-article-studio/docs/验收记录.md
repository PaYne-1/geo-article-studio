# 验收记录

最新v1.4.5增加当前Agent文字来源切换、生成前约束、聊天汇总表解析和随包内置正式规则。实际离线测试、第三方文字API、真实图片API与未验证项分开记录于[v1.4.5更新与测试](v1.4.5更新与测试.md)。

最新v1.3.0增加每任务文字来源选择与持久第三方文字API配置，见[v1.3更新与测试](v1.3更新与测试.md)。以下v1.2及之前的真实宿主结果是历史证据。

历史v1.2.0：默认名称更正为218轻便侠，启动入口精简为两个，API合并到配置任务。见[v1.2更新与测试](v1.2更新与测试.md)。下文的旧名称、旧入口和版本测试结果作为历史证据保留。

历史版本v1.1.0新增多Agent支持：源码全量114项通过，新增当前Codex有限真实交接。详见[多Agent实测记录](多Agent实测记录.md)。下文97项、Hermes会话和演示为v1.0历史验收，不能作为v1.1重新执行真实Hermes的证据。

验证日期：2026-09-14，Asia/Shanghai。操作系统Windows；开发与全量测试使用独立Python3.11.16。系统PATH默认Python3.9.13不作为本包运行环境。依赖版本见constraints-tested.txt；没有修改Hermes全局依赖。

## A. 单元和组件测试

最终全量命令：

```powershell
$env:PYTHONIOENCODING='utf-8'
$env:PYTHONUTF8='1'
& 'D:\0-AI 项目\GEO\.venv\Scripts\python.exe' -m pytest 'D:\0-AI 项目\GEO\geo-article-studio\tests' -q --junitxml='D:\0-AI 项目\GEO\artifacts\verification\pytest.xml'
```

实际结果：**97 passed in 27.48s，0失败、0跳过**。该总数包含B的离线集成测试，不包含C/D推理服务验收。纯文本输出一并附于[测试输出](测试输出.txt)，开发工作区另保留JUnit XML。

测试覆盖配置合并、中文/空格路径、源库隔离、symlink、增量索引、编码回退、中文检索、角色与会话口径、产品事实批准/冲突/失效、人工数量、学习版本/复盘/规则范围与回滚、独立审核门、图片参考bytes和URL/base64、计费账本/限额/UNKNOWN、跨进程锁、发布恢复和ZIP排除秘密等。详细对照见[32项需求对照](需求验收对照.md)。

实际可选解析器测试创建并读取了虚构DOCX、XLSX、带可提取文本的PDF。缺解析器分支在最初未安装时实际测试，最终测试另显式模拟缺少docx依赖验证降级报告。OCR没有实现或测试。

另已实际执行Python compileall成功、技能quick_validate成功（Windows须python -X utf8）、pip构建/安装成功，安装后的console form可运行，prompts资源已安装到独立环境share目录。安装器自身在实际Hermes Python3.11.16中另跑12项unittest通过；这些安装用例也包含于97项总测试，不能重复相加。

## B. 离线集成与演示

实际命令：

```powershell
& 'D:\0-AI 项目\GEO\.venv\Scripts\python.exe' 'D:\0-AI 项目\GEO\geo-article-studio\scripts\geo.py' demo --root 'D:\0-AI 项目\GEO\artifacts\离线演示最终' --mode both
```

实际LibraryIndex、ProductRegistry、Engine、文件发布与HTTP传输参与运行。测试产品名为“虚构离线演示产品”，没有把测试事实挂到218切面侠。自动模式一次人工模拟select配置3篇8图；学习模式1篇1图，包含修改、重新提交、独立LEARNING_REVIEW、人工模拟批准。两模式均COMPLETED；总共9次本地HTTP请求，每次实际上传2张参考fixture，返回Pillow生成测试图并实际解码保存。

所有用户决定、Qwen结果、视觉判定及返回图都是明确标记的模拟数据。simulation规则formal=false；普通生产Engine拒绝模拟配置。B不能证明真实Qwen写作质量、真实视觉审核或第三方收费API可用。

包外证据：`artifacts/离线演示最终/simulation_report.json`，各模式workspace/tasks及simulation_outputs；`artifacts/verification/demo-cli.json`。这些运行数据不进入安装ZIP。

## C. Hermes实际联调

实际宿主：Hermes v0.20.5；本地Qwen `qwen3.5:9b`，明确指定provider `local-qwen`，请求发往已验证本地7001端点。使用独立HERMES_HOME，没有改活动Profile。完整证据与失败过程见[Hermes真实联调记录](Hermes真实联调记录.md)。

已验证：真实skills_list/skill_view发现加载；无预加载自然语言触发实际skill_view；真实Hermes终端执行doctor/form；Qwen原样JSON经错误反馈重试后被submit-result接收，停在PREFLIGHT/WAITING_APPROVAL/revision2。

未通过或未验证：自然语言表单整段闭环的两次尝试分别因PowerShell与GitBash语法混用/空格路径未引用而超时；修订宿主说明后未重跑该完整场景。显式斜杠实时分发未单独验证；真实完整文章/图文任务和视觉推理未验证。Qwen预检文字仍有将产品图视作功能证据的语义问题，尚待人工审查，没有批准。

因此C只能报告**技能加载、终端执行和有限结构化桥接通过；完整生产未验证**，不能报告端到端全通过。

## D. 真实图片API联调

**未执行。** 用户尚未配置真实服务协议、图片规格、密钥环境变量和费用授权。本开发过程未调用真实收费图片生成，也未把本地模拟HTTP当真实服务。参考图实际上传、厂商返回格式、真实输出尺寸/外观、价格、超时计费与视觉效果仍须运行环境实际验证。

## 可交付与限制

完整源码、配置模板、提示词、中文操作说明、安装/升级脚本和构建脚本均随ZIP交付。ZIP白名单排除.env、原始客户资料、缓存、环境和演示输出。运行配置尚未填写不影响安装开发包，但会阻断依赖它们的正式生产。其他限制见[已知限制](已知限制.md)。
