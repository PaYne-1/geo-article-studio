# v1.6.5 HTML 使用方法与测试

本版新增 `docs/技能包使用方法明细.html`。该文件无需网络，可直接双击打开，也适合打印；内容覆盖完整 ZIP 安装与升级、配置任务、自动任务、选题后继续、暂停续跑、图片审核、成品目录和常见问题。

2026-09-18 实际执行 `pytest tests/test_usage_guide_html.py -q`，结果为 `1 passed`；同时以 Python 标准库 `html.parser.HTMLParser` 解析该文件，结果为 `HTML parsed`。完整安装包构建与安装冒烟结果见该版本 Release。
