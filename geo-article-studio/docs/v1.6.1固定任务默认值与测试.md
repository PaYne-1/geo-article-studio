# v1.6.1 固定任务默认值

新增非敏感配置`task_defaults`，仅保存用户明确要求每次任务沿用的目标、发布平台和目标AI。`form 开始任务`自动预填这些值，当前用户仍可在本轮覆盖。模式、文字来源和篇幅不从中继承；尤其第三方文字API仍须每轮明确选择。表单默认值不改变已经启动的任务执行快照。

本机离线执行`.venv\Scripts\python.exe -m pytest -q`：**210 passed, 2 skipped**。新增测试覆盖预填、单次覆盖、非法目标及禁止把模式放进持久默认项。本机实际配置保存与再次读取验证通过，活动任务仍处于人工选题状态。此版本未执行Hermes实际会话、真实文字API或真实图片API联调；配置保存本身没有调用付费接口。
