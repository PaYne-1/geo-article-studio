# 多Agent实测记录（v1.1.0）

日期：2026-09-14。此记录区分执行层模拟和真实宿主会话，不把不同层的结果混在一起。

| 层次 | 实际执行 | 结论和边界 |
| --- | --- | --- |
| 源码自动化 | 独立Python 3.11.16运行完整pytest | 114 passed in 34.78s，0失败、0跳过；含原有四库、学习、图像传输与发布回归及新增宿主测试 |
| 通用安装 | 无Hermes的PATH环境安装、升级、探测；四宿主同源码安装 | 19项安装用例均通过，已包含于114项，不能重复相加 |
| 通用执行协议 | 合成模型结果完成零图完整流程；人工门、视觉门和审计回归 | 属于离线模拟，不是其他厂商模型联调 |
| 当前Codex会话 | 实际读取SKILL和通用桥接，真实CLI创建隔离预检任务，本会话根据动作上下文生成结果并以JSON文件提交 | PREFLIGHT → WAITING_APPROVAL，revision 2；未批准、未进入生产，没有模拟替代本次预检模型输出 |
| Hermes | 保留v1.0的真实技能加载、终端与Qwen有限预检记录 | v1.1做了离线兼容回归，没有重跑真实Hermes会话；完整生产和视觉仍未验证 |
| Claude Code和其他Agent | 安装与契约层覆盖 | 尚未运行真实会话，不宣称工具调用、自然语言发现、模型质量或视觉审核通过 |
| 真实图片API | 未执行 | 尚无实际服务配置和费用授权；本地HTTP测试不能替代厂商联调 |

Codex实测使用独立虚构产品`host-bridge-fixture`，没有将测试资料登记为218切面侠事实。四库中仅有一份虚构聊天，其他三库为空；模型明确指出来源范围、未知角色/日期/会话号、无正式事实和规则、缺图片配置等缺项。任务编号`10f18d9502834893`，实际动作`7a3edd8873f7444284496fda828d527b`；原始命令输出、上下文、模型信封及提交结果保存在开发工作区`artifacts/agent-portability/codex`，不随安装包或Git同步运行数据。

此Codex证据仅覆盖当前会话显式读文件及CLI交接；没有验证重新安装后的原生技能发现、`$geo-article-studio`分发、完整文章、图片生成或视觉能力。producer模型名记录为`current-session`，属于自报标识，不是身份认证。

审查发现的三个问题均先以失败用例复现，再修复并通过完整回归：显式未知视觉能力不能继承旧true值；未知检查阶段拒绝；复盘审计必须记作LEARNING_REVIEW。技能校验器最初拒绝顶层compatibility，将能力说明移入兼容的metadata后实际校验成功；Python compileall也已执行成功。

自动化完整输出见[测试输出-v1.1](测试输出-v1.1.txt)。原版离线演示和Hermes真实结果仍见[验收记录](验收记录.md)与[Hermes真实联调记录](Hermes真实联调记录.md)。

## 安装包验证

从ZIP以generic安装到独立目录后，在源码目录之外运行安装副本的完整测试：**114 passed in 32.53s**，0失败、0跳过。安装器报告host=generic、protocol=geo.host.v1；没有安装或调用Hermes。最终打包后逐文件核对可执行代码和测试与该已测试副本字节一致，后续仅补充测试记录及数据权限说明。
