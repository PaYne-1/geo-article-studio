# Only variable names are shown. Configure secrets locally, never through chat.
GEO_IMAGE_API_KEY=
# Absolute path in the actual Agent runtime:
GEO_CONFIG_PATH=

# Optional third-party text API; set real value only in local persistent credentials.
GEO_TEXT_API_KEY=
